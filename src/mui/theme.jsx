import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#D3F3FF",
    },
    secondary: {
      main: "#FFBF00",
    },
    tertiary: {
      main: "#FC879F",
    },
    header: {
      main: "#54273B",
    },
    text: {
      main: "#000000",
    },
  },
});

export default theme;
